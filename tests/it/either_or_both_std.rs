//! Test for `EitherOrBoth` if the `std` feature is active

// use std::io::Cursor;
//
// use either_or_both::{EitherOrBoth, WriteIo};

#[test]
fn write() {
    // let either = EitherOrBoth::<Cursor<Vec<u8>>>::Both(Cursor::new(vec![]), Cursor::new(vec![]));
}
